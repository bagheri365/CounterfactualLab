# Data

`raw/` and `processed/` are excluded from Git except for placeholder files.

Synthetic datasets should be reproducible from code and configuration rather than committed as generated artifacts.
